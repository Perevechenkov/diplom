class Task {
     constructor(id, title, body, type) {
          this.id = id;
          this.title = title;
          this.body = body;
          this.type = type;
     }
}

export default Task;
