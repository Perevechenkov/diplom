class Task {
     constructor(id, title, type, content) {
          this.id = id;
          this.title = title;
          this.type = type;
          this.content = content;
     }
}

export default Task;
