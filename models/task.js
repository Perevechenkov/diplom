class Task {
     constructor(id, title, conditions, nextTask) {
          this.id = id;
          this.title = title;
          this.conditions = conditions;
          this.nextTask = nextTask;
     }
}

export default Task;
