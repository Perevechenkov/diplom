class Content {
     constructor(id, type, task, body) {
          this.id = id;
          this.type = type;
          this.task = task;
          this.body = body;
     }
}

export default Content;
