import React from 'react';

import { StyleSheet, FlatList, View, Text, Button } from 'react-native';

export const CheckHygieneTask = props => {
     return (
          <View style={styles.screen}>
               <Text>CheckHygieneTask</Text>
          </View>
     );
};

const styles = StyleSheet.create({
     screen: {
          flex: 1,
     },
});
